import { DownloadArquivoDirective } from './download-arquivo.directive';

describe('DownloadArquivoDirective', () => {
  it('should create an instance', () => {
    const directive = new DownloadArquivoDirective();
    expect(directive).toBeTruthy();
  });
});
