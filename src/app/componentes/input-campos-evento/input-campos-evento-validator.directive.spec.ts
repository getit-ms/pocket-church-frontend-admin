import { InputCamposEventoValidatorDirective } from './input-campos-evento-validator.directive';

describe('InputCamposEventoValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCamposEventoValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
