import { ValidaOpcoesDirective } from './valida-opcoes.directive';

describe('ValidaOpcoesDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaOpcoesDirective();
    expect(directive).toBeTruthy();
  });
});
